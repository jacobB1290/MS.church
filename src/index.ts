// Version: 1.56.1 - Cache-bust the /static/youth.jpg and /static/worship.jpg image references on /ministries by appending ?v=2. vercel.json sets Cache-Control: public, max-age=31536000, immutable on /static/* so once a browser or edge CDN cached the previous youth.jpg (the 16:9 group photo from v1.55.1), it would hold the cached bytes for a year and never revalidate even though v1.56.0 replaced the actual file on disk with the new portrait. Visible symptom: the deployed website-V2 still showed the old 16:9 group photo, while the file in the repo (and the local dev server, which has no immutable cache) showed the new portrait. Adding ?v=2 makes the request URL distinct from any cached entry; the CDN treats it as a new resource and fetches from origin. Worship image was swapped in v1.55.0 from the placeholder block (no prior cached entry to bust on that one), but the same query string was applied for consistency and in case anyone's edge had cached an earlier preview. Future image swaps at the same path should bump the v=N counter.
// v1.56.0 - Youth section image upgraded to the new portrait shot (5 girls dressed up, tree+fence backdrop). Old 16:9 group photo retired. Now a single source serves both viewports with different object-position crops. New file at public/static/youth.jpg (1320×2347 portrait, ~830 KB) pulled from the stray "Desktop youth image" commit on main. Three structural changes made it possible: (1) Section type gained imageSrcDesktop + imagePositionDesktop fields for future two-source patterns (kept the infra but Youth uses single-source); (2) refactored the image container to use position: relative + absolutely-positioned <img>/<picture> so the photo cannot grow the container to its intrinsic dimensions — previously the grid + place-items: center layout was letting the img define the row height, which made aspect-ratio: 16/9 on mobile have no effect; (3) mobile media query's position: static (originally to undo sticky) had to become position: relative to keep the positioning context for the absolutely-positioned img — otherwise it climbed up to the viewport. Object-position values: center 50% on mobile (16:9 banner from a 9:16 source, faces in upper third), center 55% on desktop (near-square crop, faces in upper third, dresses + handbags through middle). Verified across 16 viewport sizes (360 / 390 / 414 / 600 / 768 / 900 / 960 / 1024 / 1200 / 1280 / 1366 / 1440 / 1600 / 1920 / 2200 / 2560) — composition is editorial at every size, no awkward crops. Sticky positioning on the image container dropped along the way (was already redundant since v1.52.1 made image-height equal content-height via align-self stretch).\n// v1.55.3 - Removed the italic cross-link note from the Fellowship section on /ministries ("We also share a free breakfast every Sunday after the service — that's part of Community Breakfast on the Outreach page."). Note had been added in v1.51.1 to cross-reference the hospitality story over on /outreach; user asked to drop it. The Section type still supports an optional `note` field for any future section that wants one — just no section currently uses it.\n// v1.55.2 - /ministries Discipleship: Tuesday Bible Reading now renders before Thursday Bible Study. Just a swap of the two entries inside the discipleship section's entries[] array. Chronological order through the week now reads top-to-bottom (Tuesday → Thursday). JSON-LD ItemList position values in routes/ministries.ts updated to match (position 3 = Bible Reading, position 4 = Bible Study). The alternating image-side rhythm within the section flips with the new order: Bible Reading now gets image-right (since it's the first entry and the section's imageSide is "right"), Bible Study gets image-left. Anchors unchanged.\n// v1.55.1 - Youth section on /ministries gets its photo. Image pulled from the stray commit on main (1c6af5cd-07af-4f52-b9c2-026c47b202ce.jpeg from commit 68b8e1d "Youth image") via `git show origin/main:...` and saved as public/static/youth.jpg (1414×795, ~315 KB) on website-V2. The original file on main is the user's own commit; not cleaning it up since main is outside the V2 scope — left as-is. Section data: imageSrc, imageAlt, and imagePosition added to the Youth entry. The photo is a wide group shot of ~22 young people lined up shoulder-to-shoulder in front of a "2026" silver balloon-arch backdrop; faces sit in the upper-middle band. Used imagePosition: "center 40%" so the heads stay centered vertically on portrait desktop containers and the focal "2026" balloon arch lands in the middle of the crop. Verified at 7 viewport sizes (360 / 414 / 768 / 960 / 1280 / 1440 / 1920): mobile + tablet render the full 16:9 banner with all 22 youth visible (no cropping — the photo is 16:9 native); desktop portrait container (image-on-right, ~503w × 620h) crops to the middle ~12-14 youth grouped around the balloon-arch focal point with the "2026" centered, edge youth trimmed naturally. No fixes needed. (Side note observed during testing: the lazy-loaded image needs scroll-into-view before Playwright's `image.complete` check returns true; that's just a screenshot harness concern, not a user-facing issue — real visitors scroll to the section and the image loads as expected.)\n// v1.55.0 - Worship section on /ministries gets its first real photo. Image pulled from PR #47 (closed) and saved as public/static/worship.jpg (1477×831 native, ~230 KB). Section data extended: the Section type now carries optional imageSrc / imageAlt / imagePosition fields (CSS object-position; defaults to "center" when unset). When present, the section renderer emits an <img loading="lazy" decoding="async"> in place of the gray placeholder block; the existing .ministry-section-image container styling stays the same, and a new .ministry-section-image > img rule applies object-fit: cover so the photo crops to whatever aspect the container ends up at. For the worship image specifically, imagePosition is set to "center 35%" — biases the crop upward so heads and microphones stay in frame on portrait desktop containers (faces sit in the upper third of the source photo). Verified at 10 viewport sizes (360 / 390 / 414 / 768 / 960 / 1024 / 1280 / 1440 / 1920 / 2560). Mobile + tablet (≤960px): image renders as a full 16:9 banner above the content, all 5 worship singers visible. Desktop (≥961px): image fills the 5fr column (475–507px wide) in a portrait crop matched to content height (~620px), centered on the 3 lead singers (plaid suit + green-tie + cream dress) plus their microphones — natural editorial worship-trio focus, no awkward cropping. The placeholder fallback path is unchanged so the other 4 sections (Kids, Discipleship, Fellowship, Youth) still render the gray box pending real photos.\n// v1.54.2 - /ministries section order: Kids moved up to be the second section, right after Worship — so parents reading the page from the top hit "what about my kids during the service?" immediately after they've read about the service itself, instead of having to scroll past Discipleship / Fellowship / Youth to find it. New order: Worship → Kids → Discipleship → Fellowship → Youth. Anchors (#worship, #kids, #discipleship, #fellowship, #youth) are unchanged so all home-page Schedule deep-links still land correctly. JSON-LD ItemList positions in routes/ministries.ts re-numbered (1→6) to match the new on-page order so the structured data stays in sync with what visitors actually see. The "Bringing kids" tip in Sunday Gatherings still says "see Kids below" — accurate since Kids is now the very next section.\n// v1.54.1 - Two follow-on fixes from the v1.54.0 design-system pass. (1) Hero text "Mending the Broken." was too small on mobile — --text-hero clamp(52px, 8vw, 88px) floored at 52px on 414px viewports (8vw = 33px, below floor) so the hero/title size ratio collapsed to 52/36 = 1.4× on mobile vs 88/52 = 1.7× on desktop, making the hero feel un-dominant. Bumped to clamp(64px, 11vw, 96px) — bespoke steeper curve so floor is 64px on mobile (1.78× the section-title floor of 36px, restoring the desktop ratio). Token comment updated to note this is intentionally outside the regular type-scale shape. (2) Kids section on /ministries was completely broken on mobile — the v1.52.1 desktop override `.ministry-section--kids .sunday-school-content { grid-template-columns: minmax(0, 360px) minmax(0, 1fr) }` has 2-class specificity, beating the global single-class `.sunday-school-content { grid-template-columns: 1fr }` mobile rule from home-styles.ts, so the section rendered as a 360px video next to a sliver-thin text column on phones (text wrapped 2 chars per line, completely unreadable). Added an explicit mobile override at matching 2-class specificity inside the .ministry-section--kids ruleset so it collapses to a single-column banner-then-text stack with the video capped at 240px wide / 426px tall. Both fixes verified at 360/390/414px mobile widths and 1440px desktop — desktop layouts unchanged.\n// v1.54.0 - Full design system standardization pass. Components were already unified (.section-eyebrow, .section-heading, .section-card, .event-link-btn, .ministry-block etc. reused across pages), but the underlying tokens had drifted: hex colors, alpha primary-text rgba, raw font-sizes / weights / families / line-heights / letter-spacings, raw clamp() spacing values, raw border-radius / box-shadow / transition declarations were all scattered through the templates without snapping to the existing token vocabulary. This pass closes the gap.\n//\n// New tokens added to home-styles.ts :root (12 total):\n// • Text color scale (6): --text-primary, -soft (0.85), -muted (0.72), -faint (0.55), -fade (0.30), -hairline (0.10). Replaces the ~76 ad-hoc rgba(26, 26, 46, X) calls that had drifted across the codebase.\n// • Spacing T-shirt scale (7): --space-xs/sm/md/lg/xl/2xl/3xl, each a fluid clamp() tuned to the most-used spacing clusters (midpoints ~10/14/20/28/36/52/72). Replaces 110 of ~183 distinct one-off clamps used for padding/margin/gap.\n// • Border-radius scale (7): --radius-sm/md/lg/xl/2xl + --radius-pill + --radius-circle. Replaces 103 hardcoded border-radius values in home-styles.ts alone.\n// • Box-shadow elevation scale (6): --shadow-xs/sm/md/lg/xl/overlay. Replaces 53 bespoke per-card shadow declarations with a 6-tier elevation system.\n// • Motion scale (5): --motion-fast/medium/slow + --ease-standard/out-soft. Replaces 61 ad-hoc transition durations and easing curves.\n//\n// Three sweeper agents ran in parallel against the same template files. Edit tool's Read-modify-Write is atomic and the agents touched disjoint substring patterns (color hex/rgba; spacing clamp inside padding/margin/gap; font-size/family/weight/leading/tracking; border-radius/box-shadow/transition), so they did not collide on substrings. ~30% of one agent's Edit calls hit "file has been modified since read" because of concurrent writes from the other agents; each was retried after re-reading the relevant span and every intended substitution eventually landed. Final substitution count: spacing 110, typography 86, radius/shadow/motion 217 = 413 total drift fixes across 5 files.\n//\n// Files touched: src/templates/home-styles.ts (bulk), ministries-body.ts, beliefs-body.ts, visit-body.ts, about-body.ts. Untouched: outreach-body.ts (already fully tokenized), routes/privacy.ts (self-contained CSS not in token scope), routes/misc.ts. SVG presentation attributes, illustration-specific colors, bespoke animation timings (handshake-draw, video-player FLIP, countdown), accent-colored shadows (gold/red/blue gradient inputs), and the :root token definitions themselves were all left untouched per agent specs. Residual hardcoded patterns: 12 rgba(26,26,46,...) (all in SVG fill / token defs), 9 outlier font-sizes between token bands, 31 transitions with bespoke durations — all legitimate exemptions.\n//\n// CLAUDE.md "Design System" section rewritten to document the full token catalog with usage rules ("tokens are NOT suggestions"), exemption cases, and the rule for adding new tokens. Future contributors will land on tokens by default; only deliberate outliers get raw values.\n//\n// Build / sync: clean. dist/_worker.js 727.72 kB (was 725.35 — +2.4 kB from var(...) refs being slightly longer than the raw values they replaced; functionally identical output). 8-page visual regression check (home / ministries / visit / outreach / about / beliefs at desktop+mobile) showed 0 page errors and 0 unresolved var() in computed styles — every token substitution resolves cleanly.\n// v1.53.0 - Schema.org JSON-LD audit + sync. The structured-data graph had drifted from the site since the v1.51-1.52 IA work. Fixed everything in one pass.\n//\n// Home (home-head.ts): Description bullet now includes Wednesday Activity Day alongside the other four weekly gatherings. openingHoursSpecification gained a Wednesday 18:00-21:00 block. hasOfferCatalog gained two Services: "Activity Day (Fellowship)" and "Sunday School (Kids)"; "Youth Service" description updated to specify the 15+ age range; "Community Outreach" description tightened to mention monthly homeless-shelter cooking, free Sunday community breakfast, and seasonal city-wide events. Two new Event entries: #wednesday-activity-day (Wed 18:00-21:00) and #friday-youth-service (Fri 19:00-20:30, with PeopleAudience suggestedMinAge: 15 and audienceType: "High school students") — the graph now declares all 5 weekly gatherings as Events. FAQPage gained 4 new Q&As mirroring the v1.52.0 newcomer-anxiety content on /visit: dress code, parking, who greets you at the door, what about kids (Sunday School ages and drop-off), and what ages is Youth Service for. Replaced 3 broken gensparksite.com base64-upload image URLs with /static/church-building.jpg (self-hosted, won't break the rich-result thumbnail if the external host goes away).\n//\n// /visit (routes/visit.ts): Description previously said "Sunday School info, and the free breakfast that follows" — but Sunday School was folded into the 8-step Service Flow as step 3 in v1.51.0, so that phrasing was stale. Rewritten to "the 8-step service flow (welcome, worship, teaching, dismissal, breakfast), and the practical first-timer details: what to wear, where to park, who greets you at the door" — matches the actual page content as of v1.52.0. Both the JSON-LD description and the page-level meta description updated.\n//\n// /ministries (routes/ministries.ts): Previously only declared a generic WebPage + BreadcrumbList — none of the 5 ministry categories were exposed as structured data. Promoted to CollectionPage with a mainEntity ItemList of 6 Service entries (Sunday Gatherings, Thursday Bible Study, Tuesday Bible Reading, Activity Day, Youth Service, Sunday School) — each with its own description, deep-link URL into the anchor, provider reference to the canonical Church entity, Offer with price: "0" / priceCurrency: "USD", and PeopleAudience where age-relevant (Youth: 15+, Sunday School: preschool through ~5th grade).\n//\n// /privacy (routes/privacy.ts): Was the only page with no JSON-LD at all. Added a minimal WebPage + BreadcrumbList graph tied to the canonical #website and #church entities so the privacy page is at least linked into the site's overall structure for crawlers.\n//\n// Cross-page consistency preserved: every page still references the same @id values (https://ms.church/#church, https://ms.church/#website). Breadcrumb depths are correct (Home > X for direct children; Home > About > Statement of Beliefs for /beliefs). All 7 pages validated via headless Playwright (parsed every script[type="application/ld+json"], confirmed every block is valid JSON, dumped the @type list per blob — full pass in 832ms).\n// v1.52.1 - /ministries image/video sizing audit + fix. Two structural bugs were causing awkward proportions across most sections. (1) Column-width inversion on right-imaged sections. The single-entry sections used grid-template-columns: 5fr 7fr with CSS order: 2 to flip the image side, but CSS Grid auto-flow places children by source order *after* order: is applied, so right-imaged sections (Youth, and Discipleship's first pair) ended up with the IMAGE in the 7fr (larger) column and the CONTENT in the 5fr (smaller) column — image rendered ~40% wider than left-imaged sections (705px vs 503px). Fixed by adding `.ministry-section--right .ministry-section-content { grid-template-columns: 7fr 5fr }` (and the equivalent for .ministry-entry-pair--right) so the image always occupies the 5fr column regardless of side. (2) Fixed 4:5 aspect ratio + align-items: start meant the image's height was locked to (column-width × 1.25), ignoring content height. Short entries (Bible Reading at 379px, Fellowship at 489px) left the image trailing 150-390px below the content end — large dead gap. Removed aspect-ratio, switched the grid to align-items: stretch, and gave images `min-height: clamp(320px, 36vw, 460px)` + `max-height: 620px` so they stretch to match row (content) height with sensible bounds. Empty gaps gone; all sections render image-height = content-height. Pre-fix: Worship 503×629 / content 705×660, Bible Study 705×881 / 503×491, Bible Reading 503×629 / 705×379, Fellowship 503×629 / 705×489, Youth 705×881 / 503×656. Post-fix: Worship 503×620 / 705×660, Bible Study 503×468 / 705×468, Bible Reading 503×460 / 705×460, Fellowship 503×489 / 705×489, Youth 503×553 / 705×553 — image always in the 503 (smaller) column, image height always matches content height. Kids section's vertical-video frame bumped from 280×440 / max-height 440 to 360×560 / max-height 560 with align-items: start so it hugs the top of the row instead of floating mid-cell — the video was feeling orphaned next to a long tip list. Mobile is unchanged (already used 16:9 banner stack with gold tab markers; added explicit --right overrides since the new desktop rules' specificity required matching them on mobile to keep the single-column collapse).\n// v1.52.0 - Newcomer-anxiety refinements. Three connected changes:\n//\n// 1) /visit gains a new "Before You Come" section between What to Expect and the Contact CTA. Three blocks addressing the questions first-time visitors actually ask. (a) "What to Wear" — image+text pair via .ministries-pair: no strict dress code beyond modest, most folks land on the casual side (jeans and a shirt totally fine), some dress more formally; both fit in. The image is a placeholder pending a real photo of people in mixed dress styles. (b) "Parking" — right next to the building, free, no permit, short walk from lot to door. (c) "At the Door" — greeters meet everyone at the door for the main Sunday service, will help with seats/kids classroom/bathroom/etc, no pressure to introduce yourself or sign anything. Page-scoped CSS: .visit-faq-pair (image+text pair, reuses .ministries-pair on desktop, banner-stack on mobile) and .visit-faq-grid (50/50 grid on desktop, single-column stack with gold tab markers on mobile to match /outreach + /ministries editorial idioms).\n//\n// 2) /ministries Discipleship section refactored from a single shared sticky image to one image per entry — fixes the awkward layout where the same image hovered next to two different ministries. New rendering branch: when a section has 2+ entries, each entry renders as its own .ministry-entry-pair (image + .ministry-entry-content). Image sides alternate within the section (Bible Study: image right; Bible Reading: image left) for editorial rhythm. New CSS (.ministry-section-pairs, .ministry-entry-pair, .ministry-entry-pair--left/--right, .ministry-entry-image, .ministry-entry-content) mirrors the single-entry layout's typography and aspect ratios without inheriting the sticky positioning (each image only pairs with one entry now, so no sticky behavior needed). Single-entry sections (Worship, Fellowship, Youth) keep their existing rendering path verbatim.\n//\n// 3) Copy updates across /ministries to match the new /visit guidance, so the same answer never reads two different ways. Worship → Sunday Gatherings tips: "What to wear" now describes the modest-but-no-dress-code spectrum (casual side common, some more formal, both fit in); "Where to park" simplified to "right next to the building, see the lot as soon as you pull up, free, no permit needed"; new "At the door" tip describing greeters meeting everyone at the door and helping with anything; "Greeting" tip removed (folded into "At the door"). Bible Study → "First time" tip no longer says "they will sit with you"; updated to "they will get you settled". Youth Service → ages changed from "Middle school through high school (roughly 6th–12th grade)" to "15 and up — high schoolers and older"; description tightened from "designed for middle school and high school students" to "for our older students".\n// v1.51.2 - /visit handshake draw-in animation slowed back down. v1.50.4 (the dasharray bug fix) introduced an unintended side effect: stroke-dasharray was set to 1200 to be safely larger than the 893-unit path length, but that meant 307 units (~26% of the keyframe range) were spent with the dash offset past the path end — invisible "blank" time before drawing started. With ease-in-out spending most of its slow portion in that blank window, the *visible* drawing collapsed to roughly 1.8s of the 2.8s duration and the user reported it as "so fast". Tightened stroke-dasharray to 920 (just 27 units past the path length, ~0.1s of pre-draw buffer with easing) and extended the duration to 3.2s so the visible drawing fills almost the entire animation. handshake-gesture's delay bumped 3.4s → 3.8s to preserve the 600ms still-pause between Phase 1 (draw) and Phase 3 (gesture).\n// v1.51.1 - /ministries redesigned to match the editorial design language of /outreach, with intentionally different desktop and mobile layouts (not the same layout scaled). DESKTOP: each category section is a 5fr/7fr image+content magazine spread with the image alternating left/right per section for rhythm, the image position:sticky to the top: 112px so it stays anchored as the reader scrolls through multi-paragraph entries, and inline .ministry-address-trigger pills inside each entry title that open the home-hero Apple/Google/Copy dropdown. MOBILE: the same data renders as a vertical reading stack — image becomes a short 16:9 banner above the content (no second portrait inside a portrait phone), each .ministry-block gains a gold gradient tab marker above its eyebrow (matches /outreach's mobile idiom), the tip dl stacks label-above-value with a gold hairline left border per tip, and address pills get tap-sized padding. Removed the white card boxes (.ministry-entry / .ministry-grid) used in v1.51.0 in favor of clean .ministry-block / .ministry-eyebrow / .ministry-title / .ministry-text / .ministry-link tokens copied from /outreach so the design language lines up. Kids section continues to use the global .sunday-school-content + .vertical-video-frame pattern (the same one Sunday School used on /visit before it was folded into the Service Flow) so the original vertical-video design lives on here. Each entry now carries 4-5 "first-timer essentials" (What to wear / Where to park / Greeting / If you arrive late / Bringing kids / What to bring / Ages / etc.) covering the factual stuff that reduces newcomer anxiety. Clickable addresses: church address ("3080 Wildwood St" or "the church") expands the Apple/Google/Copy panel; Caffiena State Street is a direct external map link. The address-dropdown wiring JS is inlined at the bottom of ministries-body.ts (~40 lines), scoped to this page only.\n// v1.51.0 - IA reorganization (Phase 1+2+3 of the consolidated plan). Pick-one-word-per-concept taxonomy: home Outreach heading "live out our mission" → "serve Boise"; Outreach card tags Ministry/Ministry → Serve/Hospitality; Schedule tab eyebrows (Sunday Gatherings / Bible Reading / Activity Day / Bible Study / Youth Service) → category labels (Worship / Discipleship / Fellowship / Discipleship / Youth) matching the church-standard taxonomy. New /ministries hub at src/routes/ministries.ts + src/templates/ministries-body.ts: 5-category page (Worship, Discipleship, Fellowship, Youth, Kids) with anchors so Schedule tabs deep-link in (#worship, #discipleship, #fellowship, #youth, #kids). Each Schedule tab now ends with an inline "Learn more →" link into the matching ministries anchor; a centered "Explore Our Ministries" button under the Schedule mirrors the existing "Explore Our Outreach" button (same .event-link-btn.teaser-cta in a .teaser-cta-row). Sunday School folded into the /visit Service Flow: was a standalone section with a vertical-video frame, dl-facts list, and "Ask about Sunday School →" link; now slots in as Step 3 of the 8-step flow (right after Two Worship Songs, when kids leave for class) and links to /ministries#kids for the full ministry description. The final "Stay for Breakfast" step likewise links to /outreach#meals-hospitality for Community Breakfast detail. Removed the now-unused sunday-school inline-video script and VIDEO_PLACEHOLDER_SVG constant from visit-body.ts. Nav unchanged (still 4 items + Contact CTA); /ministries is reachable from every Schedule tab + the Explore button + every cross-page deep link, matching the plan's "no nav change" stance. Cross-link on /ministries#fellowship points to Outreach's Community Breakfast so the hospitality story stays anchored at /outreach (canonical) while readers reach it from either page.\n// v1.50.4 - /visit handshake SVG: real fix. The previous v1.50.3 bumped the desktop stroke-width to 3.6px which made the path thicker, but the user reported the SVG was still rendering incomplete on desktop ("not fixed"). Root cause was different: the path is styled with vector-effect: non-scaling-stroke, which makes Chromium/WebKit IGNORE the path's pathLength="100" attribute when computing stroke-dasharray. So `stroke-dasharray: 100` was being interpreted as 100 actual CSS pixels instead of 100 normalized path units — and since the actual path is 893px long across 5 subpaths, the pattern of dash-100 / gap-100 / dash-100 / gap-100 left roughly half the path strokes invisible. Whole subpaths (the inner-cuff triangles and the hand-detail curves) frequently fell entirely inside gap regions, which is the "missing" look. Fixed by replacing the pathLength-normalized dasharray with an absolute one larger than the full path: stroke-dasharray: 1200, stroke-dashoffset: 1200 → 0. 1200 > 893 so the dash never has a gap inside the path; the offset 1200 hides the path before the animation and offset 0 reveals it fully. Mobile happened to render fine before because at 240px the dash math interacted with the cuff-and-hand stroke order more forgivingly, but the new dasharray now also makes mobile robust to any future viewBox changes. v1.50.3's desktop stroke-width: 3.6px override is preserved.\n// v1.50.3 - Fix /visit handshake SVG rendering on desktop. The illustration uses vector-effect: non-scaling-stroke so the path renders at a fixed 2.4 CSS pixels regardless of SVG size. On mobile the SVG is capped at 240px wide and a 2.4px stroke looks well-weighted; on desktop the SVG is 360px wide (~1.5× larger) and the same 2.4px stroke becomes proportionally thinner, making the fine finger/wrist detail look faint and partially "missing." Added a @media (min-width: 961px) override that bumps .handshake-stroke to 3.6px so the relative line weight on desktop matches the mobile rendering. Mobile untouched.\n// v1.50.2 - /outreach "How We Serve Boise" map is extended on desktop. Same artwork — same three pins, same comet, same foothills/streets/river/greenbelt — stretched onto a 2.9:1 widescreen viewBox (1100×380) so the graphic fills the page container width instead of sitting small in the center of a 1280px column. No new elements were added; the existing composition is just repositioned to occupy the wider canvas (pins spread A→B→C across the horizontal axis, comet path loops the same A→B→C and arcs back over the foothills, river and grid extend to the new edges). Implemented as a second SVG inside outreach-body.ts gated by `.boise-map-desktop` (shown at ≥961px) — the original `.boise-map-mobile` SVG with the 600×340 viewBox is preserved verbatim and remains the only SVG visible on mobile (≤960px), so the small-screen rendering is byte-for-byte unchanged. Reverted v1.50.0's interim `max-width: 680px → 100%` change on the base `.boise-map` rule (the new two-variant approach handles desktop sizing instead). Original change log for v1.50.1 (centered video player FLIP-morph overlay for the watch section) preserved below.\n// v1.50.1 - All three watch cards play in-page via a centered video player overlay with a FLIP morph animation (desktop only). Click any thumbnail and the source card fades while a centered video frame translates+scales out of that card's bounding rect to a 1100px-wide / 88vw-capped target rect (cubic-bezier(0.32, 0.72, 0, 1), 0.6s). After ~350ms (mid-morph) the YouTube iframe is injected with autoplay so the visual cue and the playback land together. Close via the floating X (positioned above the frame), Esc, or backdrop click — the frame reverse-morphs back to the source rect, the source card fades back in, then the overlay unmounts. Body scroll is locked while the overlay is active. The backdrop uses rgba(12,12,22,0.82) with backdrop-filter blur(14px) saturate(120%) so the page reads as a film-quality dim. Cards 2 and 3 remain real <a href="..."> elements so right-click / new-tab still works; the click handler preventDefault's only on desktop (>960px). The legacy 9am-MT service-time inline auto-play path is preserved untouched — that case still loads the iframe directly into the latest card with the existing morph-to-spinner / postMessage reveal flow, so the live stream "just appears" during the service window. Mobile (≤960px) is unchanged (overlay is desktop-only; .video-card-recent siblings stay display:none). Self-contained to the watch section markup, styles, scripts, and the YouTube route. CLAUDE.md gains a "Speed is a first-class harness goal" subsection codifying parallelize-by-default / cheapest-wait / no-padding-sleeps for all tests.
// Vercel entry point (used via api/index.ts).
// Only difference from src/index.tsx (Cloudflare): the serveStatic import.
// SYNC RULE: Any change here must also be applied to src/index.tsx,
// keeping the hono/cloudflare-workers import intact in that file.

import { serveStatic } from '@hono/node-server/serve-static'
import app from './app.js'

app.use('/static/*', serveStatic({ root: './public' }))
app.use('/favicon.ico', serveStatic({ root: './public' }))

export default app

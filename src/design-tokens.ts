/**
 * Brand design tokens — single source of truth.
 * Change a value here and rebuild; every page and template updates automatically.
 */

/** Primary gold accent used for buttons, active states, links, and decorative accents. */
export const GOLD = '#856D3F';

// Version: 1.51.2 - /visit handshake draw-in animation slowed back down. v1.50.4 (the dasharray bug fix) introduced an unintended side effect: stroke-dasharray was set to 1200 to be safely larger than the 893-unit path length, but that meant 307 units (~26% of the keyframe range) were spent with the dash offset past the path end — invisible "blank" time before drawing started. With ease-in-out spending most of its slow portion in that blank window, the *visible* drawing collapsed to roughly 1.8s of the 2.8s duration and the user reported it as "so fast". Tightened stroke-dasharray to 920 (just 27 units past the path length, ~0.1s of pre-draw buffer with easing) and extended the duration to 3.2s so the visible drawing fills almost the entire animation. handshake-gesture's delay bumped 3.4s → 3.8s to preserve the 600ms still-pause between Phase 1 (draw) and Phase 3 (gesture).\n// v1.51.1 - /ministries redesigned to match the editorial design language of /outreach, with intentionally different desktop and mobile layouts (not the same layout scaled). DESKTOP: each category section is a 5fr/7fr image+content magazine spread with the image alternating left/right per section for rhythm, the image position:sticky to the top: 112px so it stays anchored as the reader scrolls through multi-paragraph entries, and inline .ministry-address-trigger pills inside each entry title that open the home-hero Apple/Google/Copy dropdown. MOBILE: the same data renders as a vertical reading stack — image becomes a short 16:9 banner above the content (no second portrait inside a portrait phone), each .ministry-block gains a gold gradient tab marker above its eyebrow (matches /outreach's mobile idiom), the tip dl stacks label-above-value with a gold hairline left border per tip, and address pills get tap-sized padding. Removed the white card boxes (.ministry-entry / .ministry-grid) used in v1.51.0 in favor of clean .ministry-block / .ministry-eyebrow / .ministry-title / .ministry-text / .ministry-link tokens copied from /outreach so the design language lines up. Kids section continues to use the global .sunday-school-content + .vertical-video-frame pattern (the same one Sunday School used on /visit before it was folded into the Service Flow) so the original vertical-video design lives on here. Each entry now carries 4-5 "first-timer essentials" (What to wear / Where to park / Greeting / If you arrive late / Bringing kids / What to bring / Ages / etc.) covering the factual stuff that reduces newcomer anxiety. Clickable addresses: church address ("3080 Wildwood St" or "the church") expands the Apple/Google/Copy panel; Caffiena State Street is a direct external map link. The address-dropdown wiring JS is inlined at the bottom of ministries-body.ts (~40 lines), scoped to this page only.\n// v1.51.0 - IA reorganization (Phase 1+2+3 of the consolidated plan). Pick-one-word-per-concept taxonomy: home Outreach heading "live out our mission" → "serve Boise"; Outreach card tags Ministry/Ministry → Serve/Hospitality; Schedule tab eyebrows (Sunday Gatherings / Bible Reading / Activity Day / Bible Study / Youth Service) → category labels (Worship / Discipleship / Fellowship / Discipleship / Youth) matching the church-standard taxonomy. New /ministries hub at src/routes/ministries.ts + src/templates/ministries-body.ts: 5-category page (Worship, Discipleship, Fellowship, Youth, Kids) with anchors so Schedule tabs deep-link in (#worship, #discipleship, #fellowship, #youth, #kids). Each Schedule tab now ends with an inline "Learn more →" link into the matching ministries anchor; a centered "Explore Our Ministries" button under the Schedule mirrors the existing "Explore Our Outreach" button (same .event-link-btn.teaser-cta in a .teaser-cta-row). Sunday School folded into the /visit Service Flow: was a standalone section with a vertical-video frame, dl-facts list, and "Ask about Sunday School →" link; now slots in as Step 3 of the 8-step flow (right after Two Worship Songs, when kids leave for class) and links to /ministries#kids for the full ministry description. The final "Stay for Breakfast" step likewise links to /outreach#meals-hospitality for Community Breakfast detail. Removed the now-unused sunday-school inline-video script and VIDEO_PLACEHOLDER_SVG constant from visit-body.ts. Nav unchanged (still 4 items + Contact CTA); /ministries is reachable from every Schedule tab + the Explore button + every cross-page deep link, matching the plan's "no nav change" stance. Cross-link on /ministries#fellowship points to Outreach's Community Breakfast so the hospitality story stays anchored at /outreach (canonical) while readers reach it from either page.\n// v1.50.4 - /visit handshake SVG: real fix. The previous v1.50.3 bumped the desktop stroke-width to 3.6px which made the path thicker, but the user reported the SVG was still rendering incomplete on desktop ("not fixed"). Root cause was different: the path is styled with vector-effect: non-scaling-stroke, which makes Chromium/WebKit IGNORE the path's pathLength="100" attribute when computing stroke-dasharray. So `stroke-dasharray: 100` was being interpreted as 100 actual CSS pixels instead of 100 normalized path units — and since the actual path is 893px long across 5 subpaths, the pattern of dash-100 / gap-100 / dash-100 / gap-100 left roughly half the path strokes invisible. Whole subpaths (the inner-cuff triangles and the hand-detail curves) frequently fell entirely inside gap regions, which is the "missing" look. Fixed by replacing the pathLength-normalized dasharray with an absolute one larger than the full path: stroke-dasharray: 1200, stroke-dashoffset: 1200 → 0. 1200 > 893 so the dash never has a gap inside the path; the offset 1200 hides the path before the animation and offset 0 reveals it fully. Mobile happened to render fine before because at 240px the dash math interacted with the cuff-and-hand stroke order more forgivingly, but the new dasharray now also makes mobile robust to any future viewBox changes. v1.50.3's desktop stroke-width: 3.6px override is preserved.\n// v1.50.3 - Fix /visit handshake SVG rendering on desktop. The illustration uses vector-effect: non-scaling-stroke so the path renders at a fixed 2.4 CSS pixels regardless of SVG size. On mobile the SVG is capped at 240px wide and a 2.4px stroke looks well-weighted; on desktop the SVG is 360px wide (~1.5× larger) and the same 2.4px stroke becomes proportionally thinner, making the fine finger/wrist detail look faint and partially "missing." Added a @media (min-width: 961px) override that bumps .handshake-stroke to 3.6px so the relative line weight on desktop matches the mobile rendering. Mobile untouched.\n// v1.50.2 - /outreach "How We Serve Boise" map is extended on desktop. Same artwork — same three pins, same comet, same foothills/streets/river/greenbelt — stretched onto a 2.9:1 widescreen viewBox (1100×380) so the graphic fills the page container width instead of sitting small in the center of a 1280px column. No new elements were added; the existing composition is just repositioned to occupy the wider canvas (pins spread A→B→C across the horizontal axis, comet path loops the same A→B→C and arcs back over the foothills, river and grid extend to the new edges). Implemented as a second SVG inside outreach-body.ts gated by `.boise-map-desktop` (shown at ≥961px) — the original `.boise-map-mobile` SVG with the 600×340 viewBox is preserved verbatim and remains the only SVG visible on mobile (≤960px), so the small-screen rendering is byte-for-byte unchanged. Reverted v1.50.0's interim `max-width: 680px → 100%` change on the base `.boise-map` rule (the new two-variant approach handles desktop sizing instead). Original change log for v1.50.1 (centered video player FLIP-morph overlay for the watch section) preserved below.\n// v1.50.1 - All three watch cards play in-page via a centered video player overlay with a FLIP morph animation (desktop only). Click any thumbnail and the source card fades while a centered video frame translates+scales out of that card's bounding rect to a 1100px-wide / 88vw-capped target rect (cubic-bezier(0.32, 0.72, 0, 1), 0.6s). After ~350ms (mid-morph) the YouTube iframe is injected with autoplay so the visual cue and the playback land together. Close via the floating X (positioned above the frame), Esc, or backdrop click — the frame reverse-morphs back to the source rect, the source card fades back in, then the overlay unmounts. Body scroll is locked while the overlay is active. The backdrop uses rgba(12,12,22,0.82) with backdrop-filter blur(14px) saturate(120%) so the page reads as a film-quality dim. Cards 2 and 3 remain real <a href="..."> elements so right-click / new-tab still works; the click handler preventDefault's only on desktop (>960px). The legacy 9am-MT service-time inline auto-play path is preserved untouched — that case still loads the iframe directly into the latest card with the existing morph-to-spinner / postMessage reveal flow, so the live stream "just appears" during the service window. Mobile (≤960px) is unchanged (overlay is desktop-only; .video-card-recent siblings stay display:none). Self-contained to the watch section markup, styles, scripts, and the YouTube route. CLAUDE.md gains a "Speed is a first-class harness goal" subsection codifying parallelize-by-default / cheapest-wait / no-padding-sleeps for all tests.
// Cloudflare Pages entry point.
// Only difference from src/index.ts (Vercel): the serveStatic import.
// SYNC RULE: Any change here must also be applied to src/index.ts,
// keeping the @hono/node-server/serve-static import intact in that file.

import { serveStatic } from 'hono/cloudflare-workers'
import app from './app.js'

app.use('/static/*', serveStatic({ root: './public' }))
app.use('/favicon.ico', serveStatic({ root: './public' }))

export default app
